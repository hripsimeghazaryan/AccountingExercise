export interface Account {
    accountId: number,
    createdAt: Date,
    id: number,
    owner: string,
    published: boolean,
    revenue: number, 
    spend: number,
    title: string,
    updatedAt: Date,
    views: number
}
