export interface Account {
    accountId: number,
    createdAt: string,
    id: number,
    owner: string,
    published: boolean,
    revenue: number, 
    spend: number,
    title: string,
    updatedAt: string,
    views: number
}